# ── App Store Connect (READY) ──────────────────────────────
APPSTORE_ISSUER_ID=44db1147-xxxx-xxxx-xxxx-xxxxxxxxxxxx
APPSTORE_KEY_ID=XXXXXXXXXX
APPSTORE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nMIGTAgEA...\n-----END PRIVATE KEY-----
APPSTORE_APP_ID=6752850093

# ── Firebase / BigQuery (READY) ────────────────────────────
# Paste entire service account JSON as a single line (escape newlines as \n)
GCP_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"respire-lyf",...}
FIREBASE_PROJECT_ID=respire-lyf
BIGQUERY_DATASET_ID=analytics_489687866

# ── Apple Search Ads (add when .p8 key is ready) ──────────
APPLE_ADS_CLIENT_ID=
APPLE_ADS_TEAM_ID=
APPLE_ADS_KEY_ID=
APPLE_ADS_PRIVATE_KEY=

# ── Google OAuth — YouTube + Google Ads (add when ready) ──
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=
YOUTUBE_CHANNEL_ID=UCxxxxxxxxxxxxxxxxxxxxxxxxx
GOOGLE_ADS_CUSTOMER_ID=
GOOGLE_ADS_DEV_TOKEN=

# ── Meta (add when system user token is ready) ─────────────
META_ACCESS_TOKEN=
META_AD_ACCOUNT_ID=act_
META_PAGE_ID=

# ── Supabase (READY — project created) ─────────────────────
SUPABASE_URL=https://jxanvdhpqzxehupxkmee.supabase.co
SUPABASE_ANON_KEY=sb_publishable_Y1PLmqV5ZTcOcPwm2xub8Q_3C5uLnj-   # anon / public key (safe to embed in HTML)
SUPABASE_SERVICE_KEY=  # ← get from Supabase Dashboard → Settings → API → service_role key (secret — GitHub Secret only)
